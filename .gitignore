__pycache__/
*.pyc

# Generated by tools/generate-feeds.py on every deploy (see deploy.yml) —
# never hand-committed, always built fresh from the current
# products-data.js / sales-log.js so it can't drift from a stale snapshot.
assets/data/
